/**
 * YouTube Data API v3 key for loading full playlist video lists.
 * Create one in Google Cloud Console (YouTube Data API v3 enabled).
 * When empty, the app falls back to the public playlist RSS feed (up to ~15 videos).
 */
export const YOUTUBE_API_KEY = '';
