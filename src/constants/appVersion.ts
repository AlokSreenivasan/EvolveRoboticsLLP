/**
 * Marketing app version shown in-app.
 * Keep in sync with:
 * - package.json `version`
 * - android/app/build.gradle `versionName`
 * - ios MARKETING_VERSION (Debug + Release)
 */
export const APP_VERSION = '1.0.0';
